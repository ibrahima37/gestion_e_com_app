spring:
  application:
    name: utilisateur-service
  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8084

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: MaCleUltraSecreteDe32CaracteresMin!
